{
  "name": "integration-api-ui-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "screenshot": "playwright test screenshot.spec.mjs --project chromium-desktop"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
